import React from 'react';
import { Layout } from '@components/Generals/Layout';
const NotFound = () => {
	return <Layout title="404">No se ha encontrado la pagina</Layout>;
};

export default NotFound;
